struct Point {
    x: Int;
    y: Int;

    func init(x: Int, y: Int) {
        self.x = x;
        self.y = y;
    }

    func printPoint() {
        print(self.x);
        print(self.y);
    }
}

final p := Point(10, 20);
p.printPoint();
