class MyList<T> implements Iterable<T> {
    func init() {}
    func iter(): Iterator<T> {
        return MyIterator<T>();
    }
}

class MyIterator<T> implements Iterator<T> {
    func init() {}
    func next(): Option<T> {
        final opt: Option<T> = Option.None();
        return opt;
    }
}

func main() {
    final list := MyList<Int>();
    for item in list {
        print(item);
    }
}
