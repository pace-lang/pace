class User {
    id: Int
    weak friend: User?
}

func main() {
    final alice := User()
    alice.id = 1
    
    final bob := User()
    bob.id = 2
    
    alice.friend = bob
    bob.friend = alice
    
    // Test WeakUpgrade
    final bobsFriend := bob.friend!
    print(bobsFriend.id)
}
