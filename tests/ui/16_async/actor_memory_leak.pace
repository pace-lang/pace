actor DummyActor {
    id: Int;

    func init(id: Int) {
        self.id = id;
    }
}

func main() {
    a := DummyActor(1);
    b := DummyActor(2);
    c := DummyActor(3);
    print("Created 3 actors without loops.");
}
