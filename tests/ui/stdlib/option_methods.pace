import "../../../stdlib/src/core/option.pace";

func main() {
    final opt := Option.Some<String>("hello");
    final opt2 := Option.None<String>();

    if opt.isSome() {
        final x := opt.unwrap();
    }

    if opt2.isNone() {
        final y := opt2.unwrapOr("default");
    }
}
