func processOption(opt: Option<Int>): String {
    return match opt {
        Some(val) => "Has value",
        None => "No value"
    };
}

func processResult(res: Result<Int, String>): String {
    return match res {
        Ok(val) => "Success",
        Err(e) => "Error"
    };
}

func main(): Int {
    final opt1 := Some(5);
    final opt2: Option<Int>  = None();
    
    print(processOption(opt1));
    print(processOption(opt2));
    
    final res1: Result<Int, String>  = Ok(10);
    final res2: Result<Int, String>  = Err("Failed");
    
    print(processResult(res1));
    print(processResult(res2));
    
    return 0;
}
