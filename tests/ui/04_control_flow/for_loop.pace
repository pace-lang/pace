import "../../../stdlib/src/collections/list.pace";

func main(): Int {
    let list = List<Int>();
    list.push(10);
    list.push(20);
    list.push(30);
    
    for item in list {
        print(item);
    }
    
    return 0;
}
