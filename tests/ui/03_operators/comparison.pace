func main() {
    let a = 10;
    let b = 20;
    
    if a == 10 { print("a is 10"); }
    if a != b { print("a != b"); }
    if a < b { print("a < b"); }
    if a <= 10 { print("a <= 10"); }
    if b > a { print("b > a"); }
    if b >= 20 { print("b >= 20"); }
}
