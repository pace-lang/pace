import "../../../stdlib/src/net/http.pace";
import "../../../stdlib/src/core/string.pace";

func main() {
    let url = "https://jsonplaceholder.typicode.com/todos/1";
    let result = httpGetRequest(url);
    
    if result.isOk() {
        let resp = result.unwrap();
        print(resp.status);
    } else {
        print("Error");
    }
}
