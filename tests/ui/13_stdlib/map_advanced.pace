func main() {
    map := Map<Int, Int>();
    
    // Test insertions and resizing
    i := 0;
    while i < 100 {
        map.set(i, i * 10);
        i = i + 1;
    }
    
    print("Map Size:");
    print(map.len());
    
    final a := map.get(10);
    print(a.unwrap());
    
    map.set(50, 500);
    print("Overwritten key 50:");
    
    // Test missing key
    missing := map.get(999);
    
    // Test delete (if implemented, otherwise wait, is map.remove() implemented? Let's check runtime)
}
