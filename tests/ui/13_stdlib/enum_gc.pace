
func test() {
    final arr := [10, 20, 30];
    final res: Result<[Int], Int> = Ok(arr);
    // When `test` returns, `res` will fall out of scope.
    // The GC should drop the `Result`, which should drop the `Array`.
}

func main() {
    test();
    print("Test Complete");
}
