func main(): Int {
    final immutableVar := 10;
    // immutableVar = 20; // This would cause an error
    
    mutableVar := 20;
    mutableVar = 30;
    
    final typedVar: String = "Hello Pace";
    
    print(typedVar);
    return 0;
}
