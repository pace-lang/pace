func main(): Int {
    final immutable_var := 10;
    // immutable_var = 20; // This would cause an error
    
    var mutable_var = 20;
    mutable_var = 30;
    
    let typed_var: String = "Hello Pace";
    
    print(typed_var);
    return 0;
}
