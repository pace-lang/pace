class Box<T> {
    value: T;
    
    func init(val: T) {
        self.value = val;
    }
}

func main() {
    final inner := Box<Int>(42);
    final outer := Box<Box<Int>>(inner);
    
    final arr := [outer];
    
    print(arr[0].value.value);
}
