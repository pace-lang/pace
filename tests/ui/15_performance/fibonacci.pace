func fib(n: Int): Int {
    if n <= 1 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

func main() {
    print("Calculating fib(35)...");
    let result = fib(35);
    print("Result:");
    print(result);
}
