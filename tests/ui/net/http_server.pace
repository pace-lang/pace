import "../../../stdlib/src/net/http.pace";

func main() {
    let server = HttpServer();
    let success = server.bind("127.0.0.1:8080");
    if success {
        print("Server bound successfully.");
    } else {
        print("Failed to bind server.");
    }
}
