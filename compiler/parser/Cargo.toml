[package]
name = "parser"
version = "0.2.0"
edition = "2024"

[dependencies]
bumpalo = { workspace = true }
ast = { version = "0.2.0", path = "../ast" }
lexer = { version = "0.2.0", path = "../lexer" }
diagnostics = { path = "../diagnostics" }
session = { path = "../session" }
