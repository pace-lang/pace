[package]
name = "parser"
version = "0.2.0-rc.1"
edition = "2024"

[dependencies]
ast = { version = "0.2.0-rc.1", path = "../ast" }
lexer = { version = "0.2.0-rc.1", path = "../lexer" }
diagnostics = { path = "../diagnostics" }
