func main() {
    var map = Map<Int, Int>();
    var i = 0;
    while i < 100000 {
        map.set(i, i * 2);
        i = i + 1;
    }
    
    print("Finished map insertions. Size:");
    print(map.length);
}
