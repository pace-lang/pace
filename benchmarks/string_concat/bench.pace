import "pace:string";
func concat_test() {
    let sb = StringBuilder();
    var i = 0;
    while i < 10000 {
        sb.append("a");
        i = i + 1;
    }
    sb.build();
    sb.free();
}

// ensure we build and then optionally free
func main() {
    concat_test();
    print("Done");
}

