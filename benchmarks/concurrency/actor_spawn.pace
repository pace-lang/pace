actor Worker {
    var id: Int;
    func init(id: Int) {
        self.id = id;
    }
}

async func main() {
    var i = 0;
    while i < 10000 {
        var w = Worker(i);
        i = i + 1;
    }
    printStr("Spawned 10000 actors.");
}
