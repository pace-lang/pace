import "../../stdlib/src/core/string_builder.pace" show StringBuilder;

func main() {
    let builder = StringBuilder();
    var i = 0;
    while i < 100000 {
        builder.append("a");
        i = i + 1;
    }
    print(builder.toString().len());
}
