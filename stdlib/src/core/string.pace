
import "../collections/list.pace" show List;

foreign func stringLen(s: String): Int;
foreign func stringConcat(a: String, b: String): String;
foreign func stringSubstring(s: String, start: Int, end: Int): String;
foreign func stringContains(s: String, sub: String): Boolean;

foreign func stringReplace(s: String, old: String, new: String): String;
foreign func stringTrim(s: String): String;
foreign func stringToLower(s: String): String;
foreign func stringToUpper(s: String): String;
foreign func stringSplit(s: String, sub: String): Pointer<CChar>;
foreign func stringSplitLen(ptr: Pointer<CChar>): Int;
foreign func stringSplitAt(ptr: Pointer<CChar>, index: Int): String;
foreign func stringSplitFree(ptr: Pointer<CChar>);
foreign func stringCharAt(s: String, index: Int): Int;
foreign func stringSkipWhitespace(s: String, start: Int): Int;
foreign func stringFindStringEnd(s: String, start: Int): Int;
foreign func stringToInt(s: String): Int;
foreign func stringToFloat(s: String): Float;
foreign func paceIntToString(value: Int): String;
foreign func paceFloatToString(value: Float): String;
foreign func paceBoolToString(value: Boolean): String;

extend Int {
    public func toString(): String {
        return paceIntToString(self);
    }
}

extend Float {
    public func toString(): String {
        return paceFloatToString(self);
    }
}

extend Boolean {
    public func toString(): String {
        return paceBoolToString(self);
    }
}

extend String {
    public func charCodeAt(index: Int): Int {
        return stringCharAt(self, index);
    }

    public func toInt(): Int {
        return stringToInt(self);
    }

    public func toFloat(): Float {
        return stringToFloat(self);
    }
    public func len(): Int {
        return stringLen(self);
    }

    public func concat(b: String): String {
        return stringConcat(self, b);
    }

    public func substring(start: Int, end: Int): String {
        return stringSubstring(self, start, end);
    }

    public func contains(sub: String): Boolean {
        return stringContains(self, sub);
    }

    public func split(sub: String): List<String> {
        let list = List<String>();
        let ptr = stringSplit(self, sub);
        let len = stringSplitLen(ptr);
        var i = 0;
        while i < len {
            list.push(stringSplitAt(ptr, i));
            i = i + 1;
        }
        stringSplitFree(ptr);
        return list;
    }

    public func replace(old: String, new: String): String {
        return stringReplace(self, old, new);
    }

    public func trim(): String {
        return stringTrim(self);
    }

    public func toLower(): String {
        return stringToLower(self);
    }

    public func toUpper(): String {
        return stringToUpper(self);
    }
}
