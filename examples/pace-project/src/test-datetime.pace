import "pace:datetime";

func runDateTimeTest() {
    print("--- Testing DateTime ---");
    let now = DateTime::now();
    print("Current Year: ${now.getYear()}");
    
    // Test Duration
    let dur = Duration(120);
    print("Duration in minutes: ${dur.inMinutes()}");
}
