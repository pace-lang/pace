class User {
    name: string
}

fn testNullSafety() {
    println("--- Testing Null Safety ---")
    let user: User? = null
    let _name = user?.name
    let safe_name = user?.name ?? "Guest"
    println("Null safety safe name:")
    println(safe_name)
}
